---
document: HX-5 Layer 0/1 Closure Evidence
status: PASS / CLOSED
date: 2026-09-15
server: hx-5
scope: post-rebuild Layer 0/1 foundation closure
---

# HX-5 Layer 0/1 Closure Evidence

## Verdict

**HX-5 Layer 0 / Layer 1: PASS / CLOSED.**

The fresh Ubuntu rebuild has been reconciled against the HX foundation baseline and survived reboot validation.

## Closure evidence

| Control | Evidence | Result |
|---|---|---|
| Hostname | `hx-5` | PASS |
| FQDN | `hx-5.hx.local.arpa` | PASS |
| Persistent IPv4 | `192.168.50.205/24` | PASS |
| Default gateway | `192.168.50.1` | PASS |
| HX DNS | `192.168.50.200` | PASS |
| HX NTP | Chrony selected `192.168.50.200` as `^*`; Reference ID `C0A832C8` | PASS |
| Chrony persistence | active + enabled after reboot | PASS |
| UFW policy | status inactive; service disabled + inactive after reboot | PASS |
| SSH runtime | active, port 22 | PASS |
| SSH persistence | `ssh.socket` active + enabled after reboot | PASS |
| Fleet SSH key | key-only access from Windows control workstation | PASS |
| NOPASSWD sudo | `sudo -n true` | PASS |
| AD machine trust | `adcli testjoin -D hx.local.arpa` | PASS |
| SSSD core service | active | PASS |
| Domain identity | `jarvisr@hx.local.arpa` resolves | PASS |
| NVIDIA driver | `595.99.02` | PASS |
| GPU 0 | RTX 5060, 8151 MiB | PASS |
| GPU 1 | RTX 5060 Ti, 16311 MiB | PASS |
| Dedicated model storage | `/srv/ollama` mounted ext4 with ~757G available | PASS |
| Reboot persistence | network, NTP, UFW posture, SSH, domain, GPU, storage all survived reboot | PASS |

## Phased-update disposition

Four Netplan packages are visible as upgrade candidates:

```text
libnetplan1
netplan-generator
netplan.io
python3-netplan
```

No packages are explicitly held. Both simulated `apt upgrade` and simulated `apt dist-upgrade` report:

```text
The following upgrades have been deferred due to phasing:
  libnetplan1 netplan-generator netplan.io python3-netplan
0 upgraded, 0 newly installed, 0 to remove and 4 not upgraded.
```

This is normal Ubuntu phased-update behavior, not a failed maintenance step, dependency defect, or operator hold. The phased packages do **not** block Layer 0/1 closure and must not be force-installed solely to make `apt list --upgradable` empty.

## Deferred known finding

After reboot, two SSSD responder socket units remain failed:

```text
sssd-nss.socket
sssd-pam-priv.socket
```

Core domain functions remain healthy. This is the existing HX4-F02 responder/socket pattern and remains **DEFERRED / NON-BLOCKING**.

## Storage enumeration note

The dedicated `/srv/ollama` filesystem appeared as `/dev/nvme1n1p3` before reboot and `/dev/nvme0n1p3` after reboot. The filesystem mounted correctly because `/etc/fstab` uses the filesystem UUID. Device enumeration names are therefore observational only and must not be treated as stable storage identity.

## Next authorized stage

Layer 0/1 foundation work is closed. HX-5 may proceed to the next build stage only under the current HX-5 runbook, preserving the verified NVIDIA `595.99.02` state and the existing UUID-based `/srv/ollama` mount.
