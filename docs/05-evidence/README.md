# Evidence

Store current validation evidence by server/application.

Suggested naming:
`YYYYMMDDTHHMMSSZ_<server>_<gate>_<description>.txt`

Evidence supports current state but does not independently override explicit owner decisions.
